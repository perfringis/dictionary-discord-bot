{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "yarn run build:seed"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "yarn run start",
    "restartPolicyType": "NEVER"
  }
}

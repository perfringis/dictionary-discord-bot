{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "yarn run seed:build"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "yarn run start",
    "restartPolicyType": "NEVER"
  }
}
